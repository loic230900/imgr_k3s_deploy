apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker
  namespace: imgr
spec:
  replicas: 2
  selector:
    matchLabels:
      app: worker
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  template:
    metadata:
      labels:
        app: worker
        app.kubernetes.io/part-of: imgr
    spec:
      # Celery task_time_limit (120s) : un resize en cours finit avant SIGKILL.
      terminationGracePeriodSeconds: 130
      # tolerationSeconds=60 (default K8s 300). Migration plus rapide sur cluster stable.
      tolerations:
        - key: node.kubernetes.io/not-ready
          operator: Exists
          effect: NoExecute
          tolerationSeconds: 60
        - key: node.kubernetes.io/unreachable
          operator: Exists
          effect: NoExecute
          tolerationSeconds: 60
      topologySpreadConstraints:
        # Jamais 2 pods worker sur le meme node en baseline; en scaling >3
        # tolere 2-1-1 puis 2-2-1 (maxSkew=1). nodeTaintsPolicy: Honor exclut
        # les nodes cordoned/NotReady pour ne pas bloquer le rolling update
        # pendant un drain et pour reschedule proprement sur les VMs survivantes.
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: DoNotSchedule
          nodeTaintsPolicy: Honor
          labelSelector:
            matchLabels:
              app: worker
      imagePullSecrets:
        - name: registry-creds
      securityContext:
        runAsNonRoot: true
        runAsUser: 999
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: worker
          image: registry.app.unistra.fr/waltzing/projet-cloud-virt/worker:placeholder
          envFrom:
            - secretRef:
                name: imgr-secrets
          env:
            - name: HOME
              value: /home/appuser
            - name: UV_CACHE_DIR
              value: /tmp/uv-cache
          livenessProbe:
            exec:
              command:
                - sh
                - -c
                - "uv run --no-dev celery -A image_api.worker inspect ping -d celery@$(hostname) | grep -q pong"
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 3
          resources:
            requests:
              cpu: 200m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 768Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
