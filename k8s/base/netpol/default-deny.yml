# Bloque tout par defaut. Les autres NetworkPolicy ouvrent ce qui est necessaire.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny
  namespace: imgr
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
